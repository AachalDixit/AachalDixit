function outputResult(result, text) {
    currentResultOutput.textContent = result;
    currentCalculationOutput.textContent = text;
  }
let number=0;
number=(number+10)*2
let text='20';
outputResult(number,text);